<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="UserRegitration.Controls.LoadingOverlay"
    IsVisible="False"
    x:Name="Root">

    <StackLayout x:Name="OverlayStack"
                 IsVisible="False"
                 HorizontalOptions="Center"
                 VerticalOptions="Center"
                 Padding="30">
        <Frame BackgroundColor="Black"
               Padding="20"
               CornerRadius="10"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               HasShadow="True">
            <StackLayout HorizontalOptions="Center"
                         VerticalOptions="Center"
                         Spacing="10">
                <ActivityIndicator x:Name="activityIndicator"
                                   IsRunning="False"
                                   Color="White"
                                   WidthRequest="75"
                                   HeightRequest="75" />
                <Label x:Name="loadingLabel"
                       Text="Loading..."
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center" />
            </StackLayout>
        </Frame>
    </StackLayout>

</ContentView>